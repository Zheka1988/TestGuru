<%= form_with model: [:admin, @test], local: true do |form| %>
  <%= render 'shared/errors', resource: @test %>
  <p>
    <%= form.label :title %> <br>
    <%= form.text_field :title, id: :test_title %>
  </p><br>

  <p>
    <%= form.label :level %> <br>
    <%= form.number_field :level, id: :test_level %>
  </p><br>

  <p>
    <%= form.label :categy_id %>
    <%= form.collection_select :category_id, Category.all, :id, :title, promt: true %>
  </p>

  <p>
    <%= form.submit %>
  </p>

  <% end %>

<!--
  <p>
    <%=# form.label :author %>
    <%=# form.collection_select :author_id, Admin.all, :id, :name_with_initial, {:selected => Admin.all.map(&:id)}, promt: true %>
  </p> -->
