  <tr>
    <td> <%= test.title %> </td>
      <td class="text-center"> <%= test.level %> </td>
      <td> <%= User.find(test.author_id).name %> </td>
      <td class="text-center"> <%= test.questions.count %></td>

      <td><%= button_to t('.start'),  start_test_path(test), class: 'btn btn-outline-secondary btn-sm' %></td>
  </tr>
</div>

